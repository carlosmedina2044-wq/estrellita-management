# Control matrix

VERIFIED means there is a test, a build check, or a committed configuration you can point at. NOT VERIFIED means the control exists in code but has no automated proof yet.

| Control | Reference | Implementation | Verification | Status |
|---|---|---|---|---|
| Household encrypted at rest (AES-256-GCM, random 96-bit IV, AAD) | MASVS-STORAGE-1 | `src/lib/crypto.ts`, `src/lib/storage/` | `src/lib/crypto.test.ts` round-trip, tamper, wrong-key, fresh-IV | VERIFIED |
| Key is random, device-scoped, never derived from a PIN | MASVS-CRYPTO-1 | `src/lib/native/device-key.ts` + `CuidalaDeviceKeyPlugin` (v2 ACL: `WhenPasscodeSetThisDeviceOnly` + `biometryCurrentSet` OR `devicePasscode`; one-shot migrate from unbound v1 / `cap_sec`; raw key bytes zeroed after `importRawKey`; passcode-required before mint; delete-safe Keychain writes) | Unit test rejects non-32-byte keys; missing-vs-cancel Keychain errors in `device-key.test.ts` | VERIFIED (key length + missing/cancel), NOT VERIFIED (Keychain attributes on device) |
| Vault is read before any key mint; never overwrite a vault with a fresh key | S1 | `hydrateHousehold` detects ciphertext first and never mints. Decrypt only after ACL `get` (`unlockHousehold`). Cancel ≠ missing (no quarantine). `persist` reuses a key that can open the vault, or mints only when no vault exists. Quarantine writes `QUARANTINED_VAULT_KEY.<timestamp>` and never deletes Keychain accounts; new keys use `cuidala-device-key-<keyId>` (envelope `v:3`). | `vault.test.ts` key-mismatch / unavailable / first-launch mint / unlock-before-decrypt / cancel / quarantine slots | VERIFIED |
| Lock flushes queued writes; persist while locked rejects; notification resync never runs on a locked session | S1 / data integrity | `lockHouseholdSession` awaits `flushHousehold` before clearing key/memory; `persist`/`write` throw `SessionLockedError` when locked with no captured key; `resyncNotifications` calls `syncScheduledNotifications` only when unlocked and onboarded | `vault.test.ts` write-then-lock flush; empty-chain lock; persist-while-locked; resync no-op while locked | VERIFIED |
| Vault key is biometric/passcode-bound; decrypt only after ACL success | MASVS-AUTH-2 | `CuidalaDeviceKeyPlugin` SecAccessControl; `get` accepts localized `fallbackTitle` from `deviceOwnerFallbackTitle()`; `face-lock.tsx` → `unlockHousehold`; lock clears CryptoKey + plaintext | Documented in `RESIDUAL_RISKS.md`; unit tests for session lock/unlock | VERIFIED (JS session), NOT VERIFIED (device ACL) |
| Lock-after uses timestamps on `pause`/`resume`, not JS timers | S3 | `app-shell.tsx`; timestamps on `pause`/`resume`, suppressed while an owner prompt is in flight; suspended during cleaner visits | Manual on device — see INCIDENT_RESPONSE 1.3 checks | NOT VERIFIED (manual) |
| Privacy screen in app switcher | S4 | `SceneDelegate.sceneWillResignActive` blur overlay; window/bridge chrome uses dynamic `#faf6ef`/`#1f1a16` (no Main storyboard) | Manual | NOT VERIFIED (manual) |
| Data never deleted on read failure | Data integrity | `hydrateHousehold` reports `corrupt`/`unavailable`/`key-mismatch`, never removes | `src/lib/storage/vault.test.ts` persist-retry; LoadFailed UI requires AlertDialog erase | VERIFIED (persist retry), NOT VERIFIED (corrupt path UI) |
| Turning app lock off requires `verifyDeviceOwner` | S5 | Settings toggle | Manual | NOT VERIFIED (manual) |
| Restore from file confirms replacement counts | S5 | `BackupPanel` AlertDialog; pre-restore envelope snapshotted to `cuidala-vault-snapshot.<timestamp>`; Settings offers one-session "Undo last restore" | `vault.test.ts` restore-then-undo; Manual AlertDialog | VERIFIED (undo), NOT VERIFIED (confirm UI) |
| Persist failure surfaces a toast once | S6 | `PERSIST_FAILED_EVENT` in `vault.ts` | Code path | NOT VERIFIED (manual) |
| New-phone restore without Keychain key is distinguishable | Data integrity | Envelope parses, decrypt fails or key missing → `key-mismatch`. Restore copies the unreadable vault to timestamped quarantine slots (never destroys Keychain), mints a new keyId account, and `await persist`s the imported household. Transient `unavailable` does not mint. | `vault.test.ts` S1b, S1c, quarantine multi-slot, unavailable restore | VERIFIED |
| Encrypted portable backup (PBKDF2-HMAC-SHA256 600k + AES-GCM, NFC passphrase) | MASVS-CRYPTO-2 | `src/lib/backup.ts`; native share writes a temp file via `@capacitor/filesystem` | `src/lib/backup.test.ts` round-trip, 210k legacy, NFC, wrong passphrase | VERIFIED |
| Legacy pre-release data migrated, PIN/account fields dropped | — | `parseStored`/`migrateHousehold` | `src/lib/storage.test.ts` | VERIFIED |
| Completion history capped at 24 months | Q8 | `rollOldCompletions` in `migrateHousehold` | Code path | VERIFIED (code) |
| No personal or address-specific data in the bundle | Privacy | `src/lib/house.ts` generic; personal room map removed | `storage.test.ts` asserts generic fallback | VERIFIED |
| App lock (Face ID / Touch ID / passcode) fails closed | MASVS-AUTH-2 | `src/components/face-lock.tsx`, `src/lib/native/biometrics.ts` via `CuidalaDeviceKey.canEvaluate` / `verifyOwner` (no third-party biometric SDK); `passcode_required` full-screen when device is insecure | Manual on device | NOT VERIFIED (manual) |
| Cleaner mode exit requires owner verification when available | — | `app-shell.tsx` `onEndVisit` | Manual | NOT VERIFIED (manual) |
| No accounts, no first-party server, no remote code | App Store 2.5.2 / 4.2 | `next.config.ts` `output: "export"`; `capacitor.config.ts` no `server.url`; `loggingBehavior: "none"` | CI greps `out/` for remote origins | VERIFIED |
| No third-party weather network from WKWebView | MASVS-NETWORK-1 | WeatherKit native plugin; CSP `connect-src 'self'`; Open-Meteo hosts removed; ZIP capped at 10 chars; attribution images ≤ 512 KB | CSP in `layout.tsx`; `Info.plist` has no Open-Meteo domains | VERIFIED |
| Location rounded to two decimals before storage | Data minimisation | `roundCoord` in `climate.ts` | `climate.test.ts` | VERIFIED |
| External links restricted to known retailers over HTTPS when arriving from outside the app | Open-redirect / phishing | `retailer.ts` `isKnownRetailerUrl`, `extractSharedUrl`; `openExternalUrl` upgrades `http:` → `https:` | `retailer.test.ts` (lookalike hosts, http, unknown host) | VERIFIED |
| Retailer pages open in SFSafariViewController, not the app WebView | MASVS-PLATFORM-2 | `native/open-url.ts` via `@capacitor/browser`; order flow keeps confirm after picker dismiss (`restock-order-state.ts`) | `restock-order-state.test.ts` pick→opened→confirm→ordered; Manual SFSafariView | VERIFIED (state machine), NOT VERIFIED (Safari UI) |
| Local notifications only; no push, no token | Privacy | `@capacitor/local-notifications`; no APNs entitlement | Config inspection | VERIFIED |
| Weekly digest is repeating | Product | `schedule.on` + `repeats: true` | `notifications.test.ts` | VERIFIED |
| Morning briefs are one-shot local notifications | Product / Privacy | `morningBriefNotifications` ids 10–16, `schedule.at` (no repeats); pushed after digest and before arrivals so `MAX_PENDING` budgets around them; silent when disabled, permission not granted, or the day has zero open duties | `notifications.test.ts` | VERIFIED |
| Lock Screen and Home Screen widgets never receive the vault key | MASVS-STORAGE-1 / MASVS-CRYPTO-1 | Vault key stays Keychain `ThisDeviceOnly` + biometry ACL (no Keychain Sharing). The extension reads a **deliberately plaintext** App Group `UserDefaults` snapshot (`group.com.cuidala.app`): `dueCount`, `doneCount`, `updatedAt`, ≤3 titles unless `privateNotifications`, run and care labels, and for the house (E3-04) the kit id, palette, season, per-window paint (`lit`/`dim`/`off`), and six sky-phase minutes. The plugin also copies the current kit's seven portrait layer files (public art, ~150 KB) from the app bundle into the container's `portraits/` folder so the extension never ships the full set. No room names, no coordinates: the phase minutes are the only location-derived value and reveal at most a coarse latitude. The `CuidalaWidget` target is in the Xcode project (`scripts/ios/add-widget-target.rb`, idempotent) and is embedded in App; the App scheme builds `CuidalaWidget.appex`. Human: register `group.com.cuidala.app` in the Apple Developer portal. | `widget-snapshot.test.ts` (builder incl. house fields); simulator build of the App scheme compiles and embeds the extension; device App Group still manual | VERIFIED (snapshot, build), NOT VERIFIED (App Group on device) |
| Widget tap opens Today | Product | `cuidala://today` URL type on the app; `appUrlOpen` / launch URL in `app-shell.tsx` → Today tab | `widget-url.test.ts` | VERIFIED (parser), NOT VERIFIED (device tap) |
| Erase-all deletes data, key, and pending notifications | App Store 5.1.1(v) | `eraseHousehold` returns `{ ok }`; requires owner verification when available; memory/key reset only after every `kvRemove`/`deleteDeviceKey` succeeds; deletes all `cuidala-device-key-*` accounts | `vault.test.ts` `erase returns ok:false and keeps memory when kvRemove throws`; Settings Face ID on device | VERIFIED (failure path), NOT VERIFIED (owner prompt on device) |
| Privacy policy and additional terms reachable in-app | App Store 5.1.1(i) | `/privacy`, `/terms`, `/how-it-works`, Settings links | Build output contains routes | VERIFIED |
| Privacy manifest matches data flows | Apple privacy manifest | `PrivacyInfo.xcprivacy` — tracking false; collected types empty (WeatherKit is Apple-collected); UserDefaults CA92.1 (vault) + 1C8F.1 (App Group widget snapshot); File Timestamp C617.1 (`@capacitor/filesystem`). Extension manifest is 1C8F.1 only. | Reviewed against this matrix and `privacy/page.tsx` | VERIFIED (files), NOT VERIFIED (Xcode target embed) |
| Face ID and location usage strings present in shipped Info.plist | App Store 5.1.1 | `Info.plist` + `es`/`pt-BR` `InfoPlist.strings`; location string states ZIP/location is sent to Apple for geocoding/WeatherKit; Cuidala has no server; `CFBundleLocalizations` en/es/pt-BR | File inspection + privacy copy in catalogs; project localizations registered | VERIFIED |
| Export compliance | App Store | `ITSAppUsesNonExemptEncryption=false`. Encryption is WebCrypto (AES-GCM, PBKDF2) provided by Apple inside WebKit, plus iOS Keychain. No custom crypto library is shipped. | Info.plist + this row | VERIFIED (config) |
| Input limits and control-char stripping | ASVS V5 | `sanitize.ts`, `migrateHousehold` | `storage.test.ts` | VERIFIED |
| Backup file input bounds (iterations, size, collection caps) | Denial of service | `BACKUP_MAX_ITERATIONS` / `BACKUP_MAX_FILE_BYTES` in `backup.ts`; `COLLECTION_LIMITS` in `migrateHousehold` | `backup.test.ts` (2e9 iterations rejected; 13 MB spaces throw before parse); `storage.test.ts` (6_000 duties → 5_000; 10 KB `triggerId` truncated; `lat: 999` → undefined) | VERIFIED |
| Dependency audit, secret scan, OSV, Semgrep | Supply chain | `.github/workflows/security.yml` | CI: `npm audit --omit=dev --audit-level=moderate`. OSV pinned to `osv-scanner-action@v2.5.1`. Known exception: `@capacitor/cli → xcode → uuid` (`GHSA-w5hq-g745-h8pq`) in `osv-scanner.toml`; not in the shipped bundle. | VERIFIED |
| Forecast, playbooks, restock math, onboarding, backups | Product | `src/lib/**` | Unit tests including weather-fire idempotence, restock invariants, climate ZIP-3 table, WeatherKit provider mock | VERIFIED |
| Day-one duties are not overdue | Product | `nextDueDate` floors first weekly/monthly due on or after `max(createdAt, today)` when there is no completion history; quarterly/yearly use `dueDate` as-is (no cadence) before `createdAt`; last completion and `installedAt` still win | `duties.test.ts` monthly/weekly creation floor; quarterly dueDate / installedAt / completion precedence; yearly not overdue before dueDate | VERIFIED |
| Starter, playbook, trigger and walk duties do not collide | Product | Every catalog task carries a `topic` (76 of 76, test-enforced); `CATALOG_TOPICS` resolves any seed title to its topic. `splitPlaybookTasks` drops a task when a live duty (any recurring one, or an open one-off) holds its topic or its exact title, or an earlier task in the batch does; an untagged task fails closed on a title match. `seedDutiesForHome` and `acceptPlaybook` use it; accepted-but-covered tasks are recorded as declined and the Do now card marks them "Already on your list". | `duty-topics.test.ts` (tag coverage, title yield, one live duty per topic after accepting every playbook on the sample home); `generate.test.ts` | VERIFIED |
| Morning brief settings persist and coerce | Product | `Household.morningBrief` defaults `{ enabled: true, hour: 8, weekdaysOnly: false }`; migrate keeps `enabled !== false`, hour int 0–23 default 8, `weekdaysOnly === true`; schema version stays 8 | `storage.test.ts` missing → defaults; `hour: 99` → 8; `enabled: "no"` → true | VERIFIED |
| Room delete requires reassignment when work remains | Product | `deleteRoomFromHousehold` no-ops on `{ action: "delete" }` if duties/supplies remain; Home editor toasts and returns | `home-model.test.ts` | VERIFIED |
| UI chrome localized (en / es / pt-BR) | App Store / i18n | Three catalogs + `react/jsx-no-literals` in components/app; seed titles localize at render | `i18n.test.ts` key parity + es/pt-BR smoke keys | VERIFIED |
| Typical costs labeled and reviewed | Q3 | `src/lib/costs/sources.json` ("national typical, 2026"); quote-only for gas/electrical/roof/structural/pest | Review this file annually | VERIFIED (file) |
| Seasonal content floor per zone | Product | `playbooks.json` climate-specific tasks | `playbooks.test.ts` zone-floor test | VERIFIED |
| Portrait-only iPhone | Product | `Info.plist` `UISupportedInterfaceOrientations` portrait; `TARGETED_DEVICE_FAMILY = 1` | File inspection | VERIFIED |
| UserDefaults vault (not Filesystem) | Q8 deferred | `@capacitor/preferences` | Documented as 1.1; skip Filesystem move this pass | ACCEPTED for v1 |
| Destructive delete offers Undo + haptic | Product | Duty/consumable delete closes sheet, removes, `hapticDestructive`, 6 s Undo toast | Model/hook restore path; Manual Undo | VERIFIED (code), NOT VERIFIED (toast UX) |
| Interactive sheets and edge-swipe back | Product | `sheet.tsx` drag-dismiss; `app-shell.tsx` left-edge interactive pop; Reduced Motion keeps tap/crossfade | Manual — see INCIDENT_RESPONSE Phase 5 extras | NOT VERIFIED (manual) |

## Not implemented (by design for v1)

Accounts, cross-device sync, household invites, server-side anything, in-app purchase / StoreKit (v1 is free; the Pro card is not shown in the shipped UI), iPad split view, and a share extension. One home, one phone. See `docs/RESIDUAL_RISKS.md` for what that leaves open.

## 1.1 follow-ups

Move the encrypted vault from UserDefaults to Filesystem. Expand playbooks toward 100 tasks. StoreKit for Cuidala Pro.
