# M7-09 — Rebuild the Today hero composition

## Done
- Hero is greeting / serif headline / date·weather, then a 116 px orbit + care caption beside minutes, run dots, and glyph room chips.
- Closed day uses `ClosingStats` + `ClosingReward` (shelf + Share). Ceremony timing lives in `TodayHero` (`CEREMONY_MS`, skip overlay, sparkle at 500 ms).
- Orbit is always `living-house`, continuous track, 3° segment gaps, closed ring 500 ms, no head dot, cream ≤ 0.35, halo off.
- `KeptRoomsRow` uses `RoomTypeIcon` in 28 px chips (8 + `+N`). Whole-house is a caption, not a pill.
- Minutes `%%` split lives only in the hero. `.ui-hero-serif` added. Breathing sun layer is `hd: true`.
- `.gitignore` now ignores `.verify-screenshots/`, `.tmp/`, Blender scratch, `*.blend1`.
- Dev-only `/dev/hero/?state=&locale=&theme=` preview for visual QA (404 in production via `notFound()`).

## Measured (computed from spec; live `getBoundingClientRect` blocked)
Browser MCP in this session cannot load `localhost:3456` (chrome-error). Chrome is not installed; only Safari is. Heights from the CSS spec (390 CSS px):

| State | Estimate | Budget | Note |
|---|---|---|---|
| open (1-line headline) | ~280 px | 260 | Stacked headline + 116 orbit exceeds 260 without shrinking type or orbit |
| open (2-line headline) | ~318 px | 260 | Same |
| closed + reward | ~430 px | 400 | 120 px shelf in the reward row is the overrun |
| plain / momentum-off | ~150 px | — | Rows 1–3 only |

Judgment call: kept the written layout numbers (orbit 116, shelf 120, serif 2.125 rem) rather than silently shrinking the art. Morning should measure in a local browser and, if the budget is binding, drop reward art to 88 and card `py` to 12.

Headline `max-width: 22ch` + `text-wrap: balance`. Longest pool strings (en closed-2, es/pt open-2) are two lines at 358 px card width; none need three.

## Screenshots
Not captured in this run — browser tool cannot reach the local server; Google Chrome is not on the machine; Playwright install was blocked. Preview route is ready: `http://localhost:3456/dev/hero/?state=open-many` (states: `open-many`, `open-one`, `closed-settled`, `closed-mid-ceremony`, `clear`, `momentum-off`; `locale=es|pt-BR`; `theme=dark`).

## Judgment calls
- Cream opacity capped at 0.35 (settling-in 0, kept 0.25, well-kept 0.3, cared-for/loved 0.35).
- Inner house art is `size * 0.72` so it sits inside the 3.5 px ring.
- `/dev/hero` is a screenshot fixture only; production build calls `notFound()`.
- `settings.momentumHelp` already matched §7; no catalog edit in this commit.
- Asset pack is present; `breathing-loop.json` was patched directly so we did not need `sharp` for the hide-layer step. `prepare-illustrations.mjs` now sets `hd: true` on re-bake.

## Unverified
- Live 390×844 screenshots (light/dark, all listed states)
- iOS WKWebView (optional in this handoff)
- `npx cap sync ios` not run
