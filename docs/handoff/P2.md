# P2 stop — Compose the portrait on Today

## Done

- Enabled momentum scene mode on Today (`sceneMode = momentumOn`): sky CSS vars, scrim, text overlay, TodaySheet −28px, scroll parallax 0.4, blur layer, compact bar >120, ambient mix, `.today-night`.
- `<PortraitScene>` replaces the hero when momentum is on: `SkyDisc`, `Clouds`, `PortraitStack` (shadow · night · day · lit · lit-blur · foliage · snow), Rive stub, `WeatherLayer` (rain/snow), Phosphor `StatusGlyphs`.
- Phase blending via `dayOpacity` / grade overlays; per-window lit clip-paths from the portrait manifest; season foliage crossfade; snow when weather is snow; minute ticker; gyro parallax (disabled under reduced motion).
- Ceremony: `houseLight` drives lit count; closed/ceremony fills all windows; sparkle-burst Lottie at the door anchor. Plain / cleaner keeps the M7-09-r2 `TodayHero` card.
- Dev fixture `/dev/portrait` with query overrides (`kit`, `palette`, `phase`, `weather`, `season`, `lit`, `closed`, `compact`, `theme`, `locale`, `reduced`).

## Proof

| Shot | Path |
|---|---|
| Type `a` × 5 phases | [a-night](P2/a-night.webp) · [a-dawn](P2/a-dawn.webp) · [a-day](P2/a-day.webp) · [a-golden](P2/a-golden.webp) · [a-dusk](P2/a-dusk.webp) |
| Type `k` golden × 3 palettes | [classic](P2/k-golden-classic.webp) · [terracotta](P2/k-golden-terracotta.webp) · [slate](P2/k-golden-slate.webp) |
| Type `u` night | [u-night](P2/u-night.webp) |
| Type `d` rain | [d-rain](P2/d-rain.webp) |
| Type `h` snow | [h-snow](P2/h-snow.webp) |
| Type `p` winter | [p-winter](P2/p-winter.webp) |
| Compact bar | [compact](P2/compact.webp) |
| Dark theme | [theme-dark](P2/theme-dark.webp) |
| es / pt-BR | [locale-es](P2/locale-es.webp) · [locale-pt-BR](P2/locale-pt-BR.webp) |
| Reduced motion | [reduced](P2/reduced.webp) |
| **Simulator (required)** | [sim-today-night](P2/sim-today-night.webp) — iPhone 17, composed night portrait on Today |

## Judgment calls

1. **Default `HomeSpec` until P3:** `resolveHomeSpec` uses stored v2 spec, else kit `a` / classic with window ids from the manifest (seed from household name). Inference picker is P3.
2. **Weather particles:** new `WeatherLayer` canvas sibling instead of extending burst-only `ParticleLayer` (ceremony sparkles stay on `ParticleLayer`).
3. **Empty window lists** (flat types `p`/`q`): fall back to whole-lit opacity `windowsLit / windowCount` instead of clip-paths.
4. **Snow layer** only when `weather.kind === "snow"` (not merely winter season); winter still swaps foliage.
5. **Garden level** for `houseLight` mapped from care level (`settling-in`→0 … `loved`→4).
6. **Simulator proof** taken on the live Today tab (night/Seattle sample household) after `cap sync ios` + XcodeBuildMCP `build_run_sim`; browser shots use `/dev/portrait`.

## Unverified / follow-ups (P3)

- Rive smoke / birds / companion / fireflies (stub only).
- `inferHomeSpec` + portrait picker + widget still export.
- Window clip-path alignment may need a visual overlay pass on types with sparse rects.
- Compact-bar fixture scrolls a local pane; production uses `.app-keep-alive` scroll as wired in Today.

**Stop.** Do not start P3 in this run.
