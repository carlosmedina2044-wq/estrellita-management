# M7-09-r2 — Fix the Today hero budget and headline

## Done
- Measured hero heights with Playwright (global Chromium via `/Users/medina/.local/share/node-tools`) at 390×844, `deviceScaleFactor: 2`, on `/dev/hero/?state=…`.
- Brought open ≤ 260 px and closed ≤ 400 px.
- Extracted `nextUpDayLabel` into `src/lib/today-copy.ts`; `heroCopyKey` returns `today.heroClear3` when clear and `nextUp` is null. Added en/es/pt-BR strings. Plain clear no longer uses an em dash placeholder.
- Applied `historyFloor(household, now)` — covered by `widgetSnapshotFor is empty when nothing is due or done` (fails on wall-clock ≠ fixture day without it).
- Committed handoff docs (`today-glows-handoff.md`, `today-glows-remediation.md`). Extended `.gitignore` for Blender `__pycache__` / preview PNG paths.
- `scripts/shot.mjs` and `scripts/measure-hero.mjs` use the global Playwright/sharp install (no new npm deps in this commit).

## Measured heights (CSS px)

| State | Light | Dark | Budget |
|---|---:|---:|---|
| open-many | 256.2 | 256.2 | ≤ 260 |
| open-one | 256.2 | 256.2 | ≤ 260 |
| closed-settled | 396.5 | 396.5 | ≤ 400 |
| clear | 256.2 | 256.2 | ≤ 260 |
| momentum-off | 131.7 | 131.7 | — |
| es-open | 256.2 | 256.2 | ≤ 260 |
| pt-BR-closed | 396.5 | 396.5 | ≤ 400 |

`openMax=256.2` `closedMax=396.5` — both within budget.

## Levers applied
All four rem levers, in order (cumulative):

1. Card padding 16 → 12 (`p-4` → horizontal 12).
2. Reward illustration 120 → 88.
3. Orbit 116 → 104.
4. `.ui-hero-serif` 2.125rem → 2rem.

After all four, open was still ~268 and closed ~410 because pool headlines wrap to two lines at card width (~332–358 CSS px) and closed stats/ledger captions were wrapping. Additional judgment calls (required to meet the measured budget):

- Settings control absolutely positioned (still 44×44) so the greeting row is text-height, not 44.
- Vertical padding tightened to `py-2` (8 CSS px) after `py-2.5` left open/closed ~0.2–0.5 px over.
- Band `mt-1.5`, care title `mt-0.5`, reward `mt-2`, right-column `gap-1.5`.
- Headline `line-clamp: 2` + `line-height: 1.05` (enforces the ≤2-line rule; closed-2 was 3 lines at 2rem).
- ClosingStats / ledger captions `truncate` / `leading-tight` so the closed band is not caption-wrapped.

## Screenshots
Sixteen captures under `.verify-screenshots/M7-09-r2/` (gitignored). Eight committed:

- [open-many-light.webp](./M7-09-r2/open-many-light.webp)
- [open-many-dark.webp](./M7-09-r2/open-many-dark.webp)
- [closed-settled-light.webp](./M7-09-r2/closed-settled-light.webp)
- [closed-mid-ceremony-light.webp](./M7-09-r2/closed-mid-ceremony-light.webp)
- [clear-light.webp](./M7-09-r2/clear-light.webp)
- [momentum-off-light.webp](./M7-09-r2/momentum-off-light.webp)
- [es-open.webp](./M7-09-r2/es-open.webp)
- [pt-BR-closed.webp](./M7-09-r2/pt-BR-closed.webp)

## Headlines
- Clear with next-up: still uses `heroClear1`/`heroClear2` with a real weekday label.
- Clear without next-up: `today.heroClear3` — “All clear. Nothing on the horizon.” (and es/pt-BR equivalents).
- `home-scene-3d.tsx` is not on this reset tip (R3); `nextUpDayLabel` is ready for it.

## Gates
`npm test && npm run typecheck && npm run lint && npm run build` — green.

## Stop
R1 complete. Do not start R2 in this run.
